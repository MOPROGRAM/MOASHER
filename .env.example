# Example .env file for local development with Vite
# Copy this to `.env.local` and replace the placeholder with your real key.
# Do NOT commit your real API key to git. If you accidentally committed a key,
# revoke it immediately and create a new one (see README for steps).
VITE_GEMINI_API_KEY=your_gemini_api_key_here